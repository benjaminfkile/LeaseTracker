/**
 * Type declarations for globals that are available in React Native 0.72+
 * and Node.js 22+, but not included in the React Native TypeScript config lib.
 */
declare function atob(data: string): string;
declare function btoa(data: string): string;
